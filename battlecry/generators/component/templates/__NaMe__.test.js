import { mount } from "@vue/test-utils";
import __NaMe__ from "./__NaMe__.vue";

describe("__NaMe__", () => {
  const wrapper = mount(__NaMe__);
  const vm = wrapper.vm;

  describe("", () => {
    it("", () => {});
  });
});
